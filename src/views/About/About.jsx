import React, { Fragment } from 'react';
import { Grid, Typography } from '@mui/material';

const About = () => {
  return (
    <Fragment>
      <Grid>
        <Typography variant='h5'>About Screen</Typography>
      </Grid>
    </Fragment>
  );
};

export default About;
