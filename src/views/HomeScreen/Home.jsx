import React, { Fragment } from 'react';
import { Grid, Typography } from '@mui/material';

const Home = () => {
  return (
    <Fragment>
      <Grid>
        <Typography variant='h5'>Home Screen</Typography>
      </Grid>
    </Fragment>
  );
};

export default Home;
