import React, { Fragment } from 'react';
import { Grid, Typography } from '@mui/material';

const Feedback = () => {
  return (
    <Fragment>
      <Grid>
        <Typography variant='v5'>Feedback Screen</Typography>
      </Grid>
    </Fragment>
  );
};

export default Feedback;
