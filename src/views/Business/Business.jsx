import React, { Fragment } from 'react';
import { Grid, Typography } from '@mui/material';

const Business = () => {
  return (
    <Fragment>
      <Grid>
        <Typography variant='v5'>Business Screen</Typography>
      </Grid>
    </Fragment>
  );
};

export default Business;
