import React, { Fragment } from 'react';
import { Grid, Typography } from '@mui/material';

const Contact = () => {
  return (
    <Fragment>
      <Grid>
        <Typography variant='v5'>Contact Screen</Typography>
      </Grid>
    </Fragment>
  );
};

export default Contact;
